# Torch_SAE

Main repo for creating a spike autoencoder in PyTorch

### Method

- Use snntorch to create a spiking network
- Use this to then try different methods of training
  - Freezing layers
  - Initialisation w/ GA and then training normally
  - etc.